/**
 * Spacing tokens — 8-point grid system.
 * Blueprint: Vol 4 §3.3.
 */
export const spacing = {
  /** 4 — inline icon padding, tight groups */
  xxs: 4,
  /** 8 — inside small cards, between icon and text */
  xs: 8,
  /** 12 — card padding, list item spacing */
  sm: 12,
  /** 16 — standard component padding, gutters */
  md: 16,
  /** 24 — section spacing, between card groups */
  lg: 24,
  /** 32 — large section padding, dashboard blocks */
  xl: 32,
  /** 48 — screen edges on tablet, major separators */
  xxl: 48,
  /** 64 — empty state spacing */
  xxxl: 64,
} as const;
